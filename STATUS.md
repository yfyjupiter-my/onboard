# STATUS

## Done
- `prd.md`, `docker-compose.yml`, `.env.example` present.
- `wireframe.html` — 3 medium-fidelity grayscale layout sets (login, checklist, material viewer, quiz).
- `themes.html` — 3 styling themes on Set B (Corporate Trust / Warm Welcome / Calm Slate dark).
- `DESIGN.md` — reverse-extracted **Warm Welcome** theme (tokens, CSS, components, prohibitions).

## Blocked
- `docker compose up` fails until `web/` (Django) + `nginx/` are scaffolded.

- `mockup.html` — production-ready Set B mockup (login, checklist, material viewer, quiz, result) in Warm Welcome theme.

- `finalize.md` — 15 resolved PRD decisions (P1–P15): django-storages+boto3, presign via `/media` proxy, no per-user assignment (lazy JoinerProgress), quiz pass_mark 80 / unlimited retakes, whitenoise, vendored htmx/Alpine, Python 3.12 / Django 5.x. None block scaffold.

- `TASKS.md` — 6 phases (P0 scaffold → P5 hardening) mapped from finalize.md P1–P15, each with acceptance criteria + audit gate.

- **Phase 0 scaffold ✅** — `web/` Django project (Django 5.2.4, py3.12), `core/` app, env-driven `settings.py` (whitenoise, security keys), `entrypoint.sh` (ensure_bucket→migrate→collectstatic→gunicorn), `nginx/default.conf` (/media→minio, /→web), vendored htmx/Alpine + hand-written `app.css` from DESIGN.md (offline, no Tailwind build). Verified via `docker compose up --build`: web healthy, home/admin/static all 200, `onboard-media` bucket created. nginx blocked only by host :80 already in use (env, not code).

- **Phase 0 audit gate ✅ PASS** — Security (`SEC-AUDIT.md`) + Code Quality (`CODE-AUDIT.md`). No blockers. Pending notes: SEC-001 nginx `/media` presign path/host mismatch (fix in Phase 2/T2.2), SEC-002 add `.gitignore` for `.env` before `git init`, SEC-003 HSTS/SSL-redirect deferred to T5.1; CODE-001 nginx bucket name hardcoded vs `MINIO_BUCKET`, CODE-002 `ensure_bucket` swallows non-404 `ClientError`.

- **Phase 1 ✅** — `core/models.py`: Material, Quiz (1:1, pass_mark 80), Question/Choice (single-correct MVP), JoinerProgress (unique user+material, nullable score/passed/timestamps). Admin: Material+Quiz/Question/Choice inlines CRUD; JoinerProgress read-only. `0001_initial` migration committed; fresh DB migrates clean, `check` 0 issues. Note: migration file root-owned on host (no sudo).

- **Phase 1 audit gate ✅ PASS** — Code Quality (`CODE-AUDIT.md`) + Business Logic (`BUS-AUDIT.md`). No blockers. Pending (Phase 3/T3.4): BUS-001 single-correct-choice invariant unenforced (guard at scoring + admin clean); BUS-002 `pass_mark` can exceed 100 → unpassable (add `MaxValueValidator(100)`).

- **Phase 2 ✅** — `core/storage.py` `MinioMediaStorage` (django-storages 1.14 `S3Storage`): uploads via `minio:9000`, browser GETs presigned against `MINIO_PUBLIC_ENDPOINT` (900s) then bucket segment rewritten to `/media/`; nginx rewrites `/media/`→`/onboard-media/` + forwards `Host` so signatures verify. Private bucket. Settings wired (path-style, private ACL, no-overwrite). New env `MINIO_PUBLIC_ENDPOINT` (default `http://localhost`). Verified in-container: upload OK, unsigned 403, signed 200. **Resolved SEC-001 (presign path/host mismatch), CODE-002 (ensure_bucket narrow except); CODE-001 addressed via nginx comment (fixed single bucket).**

- **Phase 2 audit gate ✅ PASS** — Security (`SEC-AUDIT.md`). SEC-001 resolved & verified (unsigned 403 / signed 200 / 900s). New: SEC-004 app uses MinIO **root** creds → scope to a bucket-only service account in Phase 5 (pending); SEC-005 presigned URLs are 15-min bearer tokens (acceptable for onboarding files). Carry-forward SEC-002 (.gitignore), SEC-003 (HSTS→T5.1). No blocker.

- **Phase 3 ✅** — joiner flow in `core/views.py` (checklist, material_view, quiz) + templates (login, checklist, material, quiz, result, `_topbar`). Django built-in Login/Logout (logout POST), `@login_required` on all joiner views. Checklist left-joins progress (no lazy create); material_view `get_or_create` + state machine (no-quiz→completed, quiz+not_started→viewed), presigned `<iframe>`/`<video>` src; quiz scores `round(correct/total*100)` from prefetched choices, pass_mark gate, unlimited retakes, empty-quiz guard. Result screen plain POST form (htmx skipped). Deleted orphan `home.html`. Verified via test client: login gate, completion state machine, fail 50%/pass 100%, retake, joiner no admin.

- **Phase 3 audit gate ✅ PASS** — Business Logic (`BUS-AUDIT.md`) + Security (`SEC-AUDIT.md`). No blocker. New: **BUS-003** failed retake after completion leaves `status=completed` but `passed=False` → **fix before T4.1** (CSV reads these); BUS-004 quiz-pass-without-view is acceptable (matches P5); SEC-006 no login throttle (mitigated by planned Cloudflare Access). Still pending: BUS-001 (single-correct choice), BUS-002 (pass_mark>100). Verified-good: authz/`@login_required`, no IDOR, CSRF on all forms, auto-escaping, correct answers not shipped to client.

- **BUS-003 fixed** — `quiz()` now guards the overwrite: a failing retake on an already-`completed` material is a no-op, keeping the passing `score`/`passed`/`completed_at`. Exports are consistent.

- **Phase 4 ✅** — T4.1 `JoinerProgress` admin action "Export selected as CSV": stdlib `csv` → `HttpResponse(text/csv)`, `select_related(user, material)` (no N+1). Columns: joiner name (full name or username), email, material title, status, score, passed, completed_at (ISO); None→empty cell. `check` 0 issues.

- **Phase 4 audit gate ✅ PASS** — Compliance/Accessibility (`COM-AUDIT.md`) + CSV-injection (Security). COM-001 PII export is admin-only/data-minimized (acceptable); COM-002 a11y N/A (no new UI). **SEC-007 CSV formula injection fixed** — `_csv_safe()` quotes cells starting `= + - @`/control chars, applied to all fields; verified by inline asserts. No blocker.

- **Phase 5 ✅** — T5.1 prod-only security block (`if not DEBUG`): SECURE_SSL_REDIRECT + HSTS 1yr/subdomains/preload (rest was already wired) → **resolves SEC-003**; `check --deploy` clean except W009 (placeholder SECRET_KEY = operator value). T5.2 `README.md` (config table, run, createsuperuser, CSV export, Cloudflare Access + SEC-004 prod notes, dev/test cmds). T5.3 `core/tests.py` 7 tests (csv_safe, quiz scoring + BUS-003 state machine, no-quiz completion, presign shape) — all green. No volume mount → rebuild image before tests.

- **Final audit gate — ⚠️ 4 open actions (none crash/RCE-class)**. Verified-good: authz/CSRF/presign/no-XSS, quiz scoring + BUS-003, prefetch/select_related (no N+1), HSTS/SSL (SEC-003 resolved), a11y basics. Open: **SEC-002** add `.gitignore` before git init; **COM-003** base.html loads Google Fonts CDN (privacy/GDPR + breaks offline claim); **BUS-001** single-correct-choice unenforced (0-correct = unpassable trap); **BUS-002** pass_mark>100 unpassable (MaxValueValidator). Accepted/deferred: SEC-004 (MinIO service account, README-noted), SEC-006 (login throttle → Cloudflare Access). CODE/RUN/ROB: clean, no action. `check --deploy` clean except W009 (placeholder SECRET_KEY = operator value).

- **Final-gate fixes ✅ (all 4 applied & tested)** — SEC-002 `.gitignore` added; COM-003 Google Fonts CDN removed, Inter+Fraunces vendored locally under `static/fonts/` (offline restored, no IP leak); BUS-002 `pass_mark` `MaxValueValidator(100)` + migration `0002`; BUS-001 `ChoiceInlineFormSet` enforces exactly-one-correct. `manage.py test core` **11/11**; `check --deploy` clean except W009 (placeholder key); no external URLs in templates/css; fonts collected+hashed. No open audit blockers remain.

- **CODE-AUDIT cleanup ✅** — CODE-001 nginx bucket drift resolved: `nginx/default.conf` → `default.conf.template` mounted at `/etc/nginx/templates/`, image envsubst substitutes `${MINIO_BUCKET}` at startup; compose passes `MINIO_BUCKET` to nginx. Storage `url()` rewrite and nginx rewrite now both derive from one env var (no drift). CODE-002 confirmed already fixed (`ensure_bucket` re-raises non-404). Both marked ✅ in `CODE-AUDIT.md`.

- **Phase 2 security re-audit ✅ PASS** (post CODE-001) — presign/expiry/private-bucket surface sound. **SEC-008 fixed** — pinned `NGINX_ENVSUBST_FILTER=MINIO_BUCKET` so envsubst can't blank the presign-signing `Host $host` header. Carry-forward unchanged: SEC-004 (MinIO root creds → service account, Phase 5), SEC-005 (15-min bearer URLs, accepted). Recorded in `SEC-AUDIT.md`.

- **Checklist card content ✅** — cards now reflect the uploaded material: cover shows a `PDF`/`▶` glyph with type-specific gradient (`type-{{material.type}}`), kicker appends `· Quiz` when a quiz is attached. `checklist()` view computes `quiz_material_ids` (one query) and passes `has_quiz` per row.

- **Explicit "Mark complete" button ✅** — no-quiz materials no longer auto-complete on view; viewing marks `viewed` ("In progress") and the joiner clicks **Mark complete** on the material detail page (bottom-right, alongside where the quiz button sits) to finish. New `mark_complete` view (POST-only, `@login_required`, CSRF) + `material/<pk>/complete/` URL; 404s for quiz materials so the pass_mark gate can't be bypassed. Button rendered as a sibling `<form>` (can't nest in the tile `<a>`), shown only on non-completed no-quiz cards. Quiz flow unchanged. `NoQuizViewTests` updated (view→viewed, button→completed, quiz→404); `manage.py test core` **13/13**.

- **Video/PDF playback fix ✅** — after host port moved 90→8080, presigned media URLs 404'd (signed against `http://localhost`:80, browser on :8080) → `<video>` "no supported format". Fixed: `.env` `MINIO_PUBLIC_ENDPOINT=http://localhost:8080`; nginx `/media/` now forwards `Host $http_host` (keeps port) not `$host` (strips it) so MinIO signature verifies. Verified 206 `video/mp4` range response end-to-end. **If host port changes again, update `MINIO_PUBLIC_ENDPOINT` to match.**

- **Review-gated "Mark complete" ✅** — button greyed/disabled until the joiner reviews the material. Alpine `x-data="{reviewed}"`; `:disabled="!reviewed"`; CSS `.btn:disabled`. **Video**: enables on native `@ended`. **PDF**: native iframe can't expose scroll (cross-origin), so PDFs now render via **vendored PDF.js** (`static/vendor/pdf.min.mjs` + `pdf.worker.min.mjs`, offline) page-by-page into a scroll container; Alpine `@scroll` unlocks at the last page (short/1-page PDFs unlock immediately via a synthetic scroll event). Non-PDF/corrupt files (or images/`.md` mis-typed as `pdf`) `.catch` → fall back to native `<iframe>` and unlock (can't scroll-gate). `.mjs` served as `text/javascript` by whitenoise. UX gate only — `mark_complete` view unchanged. Browser-verified: 3-page PDF gates then unlocks on scroll; PNG-as-pdf falls back + unlocks. `material.html`, `app.css`, `static/vendor/pdf*.mjs`.

- **LAN access / CSRF (operator config, not code) ✅ diagnosed** — accessing from another device gave "CSRF verification failed" on login. Fix via `.env`: add the device-facing origin to `DJANGO_CSRF_TRUSTED_ORIGINS` (exact scheme+host+port, e.g. `http://192.168.x.x:8080`) and the host to `DJANGO_ALLOWED_HOSTS`. Note: over plain `http` with `DEBUG=False`, `SESSION/CSRF_COOKIE_SECURE` mark cookies Secure so they aren't sent → CSRF fails regardless; keep `DEBUG=True` for LAN/http or serve via https.

- **LAN media playback (operator config, not code) ✅ diagnosed** — dashboard loads on another device but card content (video/PDF) won't render. Cause: media is presigned against `MINIO_PUBLIC_ENDPOINT=http://localhost:8080`; on the other device `localhost` = that device, so media 404s. Fix in `.env`: `MINIO_PUBLIC_ENDPOINT=http://<server-LAN-IP>:8080`, then `docker compose up -d`. **`MINIO_PUBLIC_ENDPOINT` and `DJANGO_CSRF_TRUSTED_ORIGINS` must use the same host the browser types** — presigned URLs are signed for one host, so pick one address (LAN IP) and use it everywhere.

- **`EndpointResolutionError` on material view ✅ fixed (operator config)** — `.env` had a comma-list in `MINIO_PUBLIC_ENDPOINT`; it is a **single boto3 endpoint URL**, not a list (only `DJANGO_ALLOWED_HOSTS` / `DJANGO_CSRF_TRUSTED_ORIGINS` accept commas). Set to `http://192.168.100.210:8080` (the LAN IP). Presigned URLs are signed for exactly one host, so **every device — including the server — must browse `http://192.168.100.210:8080/`**, not `localhost`.

- **QA check (Business Logic + Security) on the review-gate surface ⚠️ 4 open** — `BUS-AUDIT.md` / `SEC-AUDIT.md`, 2026-07-27. **SEC-009 (blocking for LAN/prod)** MinIO console `9001:9001` exposed with root creds → bind `127.0.0.1`. **SEC-010** PDF fallback iframe is same-origin + unsandboxed → uploaded HTML/SVG runs as the joiner (fix: `frame.sandbox=''` + nginx `nosniff` on `/media/`). **BUS-005** `mark_complete` accepts a POST with no proof the material was opened (fix: require existing `status=VIEWED` row). **BUS-007** unplayable `type=video` never fires `ended` → joiner stuck forever (fix: `@error="reviewed = true"`). Accepted: BUS-006 (PDF gate unlocks on any render error — UX-only gate). SEC-011 pdf.js pinned at 4.6.82, post-CVE-2024-4367, note version in README. Verified good: authz/CSRF/no-IDOR on `mark_complete`, staff blocked from joiner login, escapejs, offline vendoring, presign surface unchanged.

- **QA fixes ✅ (all 4 applied & verified)** — SEC-009 MinIO console → `127.0.0.1:9001`; SEC-010 fallback iframe `sandbox=''` + nginx `/media/` `nosniff`; BUS-005 `mark_complete` requires an existing `VIEWED` progress row (never-opened → 404, completed → idempotent); BUS-007 `<video @error>` unlocks so a bad video can't strand the joiner. Plus SEC-011 pdf.js 4.6.82 + htmx/Alpine versions recorded in README. `manage.py test core` **16/16**, `nginx -t` ok, stack up (`/` 302 → login). Only BUS-006 remains accepted (review gate is client-side by design).

- **Login loading screen ✅** — `loading-effect.html` playground (3 variants: orbit mark / skeleton / curtain wipe); user picked **curtain wipe**. Wired into `registration/login.html`: full-screen teal overlay (dots + indeterminate bar + `clip-path` curtain reveal) shows on submit, then the POST fires after a **3s hold** (`data-sent` guard prevents double-submit; no-JS posts normally, no delay). Note total wait = 3s + server round-trip, and failed logins also sit the full 3s. **Reduced-motion fix**: a tester's machine showed a dead solid screen because `prefers-reduced-motion: reduce` killed every animation — now dots/bar keep an opacity heartbeat (no travel) and the curtain fades via the same keyframes so it also clears where `clip-path` is unsupported. Same fix mirrored in `loading-effect.html`.

- **Loading screen → orbit mark ✅** — switched from curtain wipe to the **orbit mark** variant: light `--bg` overlay, SVG progress ring sweep, two counter-rotating orbit dots, pulsing "O" mark, brand + "Signing you in…". Curtain markup/CSS deleted. Submit flow unchanged (3s hold, `data-sent` guard, no-JS posts normally). Reduced motion: animations off, ring parked mid-sweep, message keeps an opacity heartbeat.

- **Login page background ✅** — `login-bg.html` playground (3 options: dot lattice + flat shapes / tiled onboarding icons / diagonal stripes + arcs); user picked **A, dot lattice**. Wired into `registration/login.html` as a fixed `#login-bg` layer (`aria-hidden`, `pointer-events:none`, `z-index:0`): 22px muted dot grid + four flat teal/amber shapes (circle, ring, pill, rounded square) via `color-mix` on DESIGN.md tokens. Card raised to `z-index:1`. No gradients/JS/images; loading overlay (`z-index:50`) still covers it.

- **Admin CSV export ✅ verified working (no code change)** — reported as "cannot download quiz results". Reproduced end-to-end through nginx (real login + POST): `export_as_csv` returns the CSV correctly. Two operator gotchas: (1) the admin action needs rows **ticked** — with nothing selected Django just re-renders with "Items must be selected"; (2) `/admin/` is only reachable by **staff** accounts (`admin`), while the joiner site rejects staff (`JoinerLoginForm.confirm_login_allowed`, `views.py:17`) — two accounts, two doors. Briefly set `is_staff=True` on `chris.goh`, which locked it out of the joiner login; reverted. **`chris.goh` privileges cleaned:** `is_superuser` False (stray bit on a joiner) and its 44 explicit `user_permissions` cleared — inert on a non-staff account; re-grant via admin if ever promoted.

- **README rewritten as full operator guide ✅** — prerequisites → clone → `.env` (every var + LAN example) → `docker compose up --build` → superuser → materials/quizzes → joiners → joiner flow → CSV export; plus production deployment (TLS, `check --deploy`, Cloudflare Access on `/admin/`, MinIO service account, SSH tunnel to `:9001`, backup of `pgdata` + `minio-data`), troubleshooting table, dev commands, doc map. Two "read first" warnings hoisted from past incidents: (1) **one address everywhere** — `MINIO_PUBLIC_ENDPOINT` is a single URL and must match `DJANGO_ALLOWED_HOSTS` / `DJANGO_CSRF_TRUSTED_ORIGINS` / the address bar incl. port; (2) **two accounts, two doors** — `/admin/` staff-only, joiner site non-staff-only. Also documents the `DEBUG=False`+plain-http secure-cookie CSRF trap, port-change → re-set `MINIO_PUBLIC_ENDPOINT`, "tick rows before the CSV action", and rebuild-image-after-code-change. Corrected stale facts: app URL is `:8080` (was `http://localhost`), 16 tests, fonts vendored under `static/fonts/`.

- **Landing page background ✅** — `landing-bg.html` playground (3 animated options: parallax dot drift / floating shapes / flowing wave lines); user picked **B, floating shapes**. Wired into `checklist.html` as five `.bgshape` elements + CSS in `app.css`: flat teal/amber circle, ring, pill, rounded square and small ring, each on its own bob/rotate keyframe (13–34s, transform-only). `position:fixed; z-index:-1` inside `.wrap`'s stacking context → always behind cards, no layout/scroll impact. `prefers-reduced-motion` now also kills `animation` (was transitions only). Scoped to the checklist page; login keeps its dot lattice.

- **Typography → Optical Grotesque ✅** — `fonts.html` + `fonts-modern.html` playgrounds (6 options total, latter via ui-ux-pro-max); user picked **E · Optical Grotesque**. **Bricolage Grotesque** (variable, vendored `web/static/fonts/bricolage.woff2`, 205KB) replaces Fraunces for all titles/`.title`/card headlines: weight 800, `-.04em`, `line-height:.95`, `font-optical-sizing:auto`, `text-wrap:balance`. `<em>` inside a title = accent word at weight 500 + `--primary` (not italic) — applied to login ("Welcome / *Onboard*"), checklist, quiz, result. Body Inter 14→16px/1.6; card kickers now amber, 11px/700/`.1em` uppercase. `fraunces.ttf` deleted; DESIGN.md §2/§4/§6 + README rewritten. **Two traps recorded in DESIGN.md §2:** (1) Google's legacy-UA `.ttf` download is a *static* instance — `font-weight` does nothing; use the variable file from google/fonts. (2) `font-variation-settings` outranks `font-weight` and resets unlisted axes to default — never use it here. Both hit during this change and were caught by screenshot diff against the playground. `manage.py test core` **16/16**; login page browser-verified at 1280px and 390px.

- **Mobile check + 2 DESIGN.md violations fixed ✅** — checklist browser-verified at 390×844 (2× DPR, mobile UA) and 768px: no horizontal scroll, 34px title wraps cleanly with the teal accent word on line 2, cards stack full-width. Fixed: (1) card covers used `linear-gradient` (§1/§6 ban gradients) and text glyphs `▶`/`PDF` (§4 = inline SVG only) → flat 10% token tints + Lucide-style SVG (teal file / amber play, 30px); type is still stated in the kicker so colour is never the only signal. (2) `.bgshape` %-positions put every shape but one off-canvas on a phone → `max-width:600px` block shrinks and re-anchors all five. `manage.py test core` 16/16. Note: authenticated pages were checked by minting a server-side session for the existing `chris.goh` user (no password change, no new account) and deleting it after.

- **Material + quiz pages mobile-checked ✅** — 390×844 (2× DPR, mobile UA): no horizontal scroll, no console errors on any of video material / PDF material / quiz. Video player, PDF.js container and buttons all fit. One cosmetic fix: **Bricolage Grotesque's capital `Q` has a long horizontal tail that runs under the following letters**, so the teal `<em>Quiz</em>` accent read as an accidental underline — quiz title now accents the material name instead (`Quiz — <em>{{ material.title }}</em>`, `quiz.html:6`). Same glyph will show under any material title starting with Q; it renders fine, just looks unusual. PDF text is small at 390px (A4 fitted to width) — left as-is, pinch-zoom covers it. 16/16 tests.

- **Material type "Link" ✅** — HR can now add a **URL** material alongside PDF/Video. `Material.type` gains `link`; new `url = URLField(blank=True)`, `file` now `blank=True`, `clean()` requires exactly the right one per type (admin form enforces it), `source_url` property returns the URL for links / the presigned file URL otherwise. `material_view` passes `material.source_url`; `material.html` renders links in a same-tab `<iframe>` (`referrerpolicy="no-referrer"`, unlocks **Mark complete** on `load`) with a permanent "Page blank? Open link" escape hatch below it; `checklist.html` gets a link-glyph SVG. Migration `0003_material_url` (additive, no data touched). **Known limit:** sites sending `X-Frame-Options`/frame-ancestors CSP (Google, LinkedIn, most corporate sites) render blank in the iframe — there is no reliable cross-origin way to detect that (the frame still fires `load`), hence the always-visible fallback link rather than an auto-swap. Prefer embeddable URLs (Drive `/preview`, own intranet). Review gate for links is weak by nature (unlocks on frame load) — attach a quiz if proof of reading matters.
  - **Two follow-up fixes** — (1) the `{# … #}` note was written across two lines and Django's `{#` comment is **single-line only**, so it printed verbatim on the page; now one line. (2) HR pasted a YouTube `/watch` URL, which sends `X-Frame-Options: SAMEORIGIN` and framed blank — `models.embeddable()` (stdlib `urlparse`) now rewrites `youtube.com/watch?v=` and `youtu.be/` to `/embed/<id>`; everything else passes through untouched. `referrerpolicy="no-referrer"` dropped (YouTube refuses to play without a referrer), `allowfullscreen` added. Rendered page verified: no comment leak, `src` is the embed URL. `manage.py test core` **18/18**.

  - **YouTube error 153 fixed** — embeds failed with "Video player configuration error" because Django's default `SECURE_REFERRER_POLICY = "same-origin"` strips `Referer` cross-origin and YouTube rejects the request. Fixed with `referrerpolicy="strict-origin-when-cross-origin"` on the link iframe (per-frame override, no global setting change). Browser-verified playing. The "Page blank? Open link" escape hatch was then removed at the user's request — a site that blocks embedding now just renders blank, so **prefer embeddable URLs** (YouTube, Drive `/preview`, intranet). 18/18 tests.

## Next
- **Deploy prerequisites (operator, not code):** set real `.env` secrets (clears W009), `git init` (`.gitignore` ready), scope MinIO service account (SEC-004), put Cloudflare Access on `/admin/` (SEC-006/P14). App code is deploy-ready.

